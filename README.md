# Simple Firebase Live Chatting
- build using Bootstrap 5, Jquery 3 & Webpack

# How To Setup
- run `npm install`
- run `npm run watch` to observe js script changes
- run `npm run build` to compile script
